from pathlib import Path

# Helper constant to refer to the project root directory.
# (e.g. to build paths inside the project like this: BASE_DIR / 'subdir'.)
BASE_DIR = Path(__file__).resolve().parent.parent.parent
